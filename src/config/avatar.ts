/**
 * Fill this in with your own Ready Player Me avatar URL, e.g.
 * "https://models.readyplayer.me/<your-avatar-id>.glb?morphTargets=ARKit,Oculus%20Visemes"
 *
 * 1. Create a free "Full Body" avatar at https://readyplayer.me
 * 2. Copy the .glb link it gives you (append the morphTargets query param above so
 *    facial blendshapes are included — later milestones drive those for expressions).
 * 3. Paste the URL below.
 *
 * Left empty by default: this sandbox has no network access to readyplayer.me, so no
 * sample avatar could be downloaded or verified here.
 */
export const DEFAULT_AVATAR_URL = '/avatar.glb';
